# Calendar Improvements

Add functionality to calendar so if user click on event it opens at `Profile` page.

Also improve form `Метадані документа` where remake `Строки виконання` date in the same style as `Дата документа` that has three fields Day Month Year.

To the `Profile` page table add button to each raw that deletes selected file to the system trash bin

To the `Profile` page after button `Додати метадані` add button `Відкрити у Finder` - this button must open system Finder at location of current folder